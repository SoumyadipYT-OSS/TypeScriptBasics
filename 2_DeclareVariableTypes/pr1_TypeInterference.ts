let x: Number;  // Explicity declares x as a number type
let y = 1;   // Implicity declares y as a number type
let z;   // Declares z without initializing it or type
let s: String;  // Explicity declares s as a string type